# 세션 메모리 — 2026-06-17 (main)

> 커밋: b12f69b codegraph-first follow-up · RIPER: REVIEW

## 작업 1: 서브에이전트 인식 hook 강제 & 탈옥 봉합 (PLAN→EXECUTE)

- 분석 문서: `docs/Try_SubagentHookEnforcement.md` (§0~7 — hook은 서브에이전트에도 발화하나, 콜드스타트 토큰세금으로 서브 기본 제외 유지. 단 hook porosity 수정은 메인 단속용으로 유효)
- PLAN: `.claude/memory-bank/main/plans/main-2026-06-17-subagent-hook-enforcement.md` (10 스텝, Plan Scope Lock)
- 결정 (cxt5.md 확정):
  - **D1**: alt-A 채택 — 서브에이전트(`agent_id` 존재)→`deny`, 메인→`ask`. `CG_GATE_MAIN_STRICT=1` 토글로 메인도 deny(기본 **off**).
  - **D2**: 비코드 Glob(.prefab/.unity/.sh) 통과 · 서브 광역 `**/*` sweep→deny · bash `cat/head/tail .cs`·PS `Get-Content .cs` 봉합 · 순수 Grep 식별자 패턴 봉합.

### 완료 STEP 1~6 (모의 JSON 17/17 검증 통과)
- `sonar-guard.sh` / `sonar-guard-powershell.sh`: `agent_id`→deny/ask 분기, `cat/head/tail`·`Get-Content` 소스 매처 추가, `${CTX_LABEL}` 위반 로깅
- `codegraph-gate.sh`: D1 전체 + D2(광역 Glob deny·Grep 식별자 봉합·비코드 통과·codegraph 선행 회귀 0)
- `tool-history-recorder.sh`: 로그 형식 `<epoch> <TOOL> <MAIN|agent_type>`
- `subagent-dispatch-gate.sh`(신규) + `settings.json` `"matcher":"Task"`: Task 디스패치→ask + 토큰세금 경고
- `violation-count.log` 형식: `<epoch> <MAIN|agent_type> <type:detail>`

### 미완 (사용자 승인 대기)
- **STEP 7**: 18개 에이전트 frontmatter에 codegraph_* 9종 부여 (B — A와 동반 필수, 교착방지)
- **STEP 8**: CLAUDE.md 서브에이전트 정책 본문 + memory 정정 (헌법급, 정밀 편집)

## 작업 2: try 스킬 저장 경로 변경
- `docs/Try_{주제}.md` → `docs/try/Try_{주제}.md` (디렉토리만 하위로, 파일명 패턴 유지)
- 3개 SKILL.md 수정 완료: `.claude/skills/try/`, `.agents/skills/try/`, `.trae/skills/try/`
- 미결: 기존 `docs/Try_SubagentHookEnforcement.md`를 `docs/try/`로 이동할지 (보류 중)

## 미결 사항 (다음 세션)
- STEP 7·8 승인 여부
- 기존 `docs/Try_*.md` → `docs/try/` 이동 여부
- D4 디스패치 도구명: 현 환경은 `Agent`, Claude Code 표준은 `Task` — settings는 `"Task"` 매처로 배선(실제 발화명 EXECUTE 시 재확인 필요)

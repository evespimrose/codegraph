# sync manifest — what counts as "the workflow"
# One path per line, RELATIVE to the source repo root.
# Directories are copied recursively; files are copied verbatim.
# '#' = comment, blank lines ignored.
#
# EXCLUDED BY DESIGN (this repo's project-specific runtime state — NEVER synced):
#   .claude/state/              violation logs, output-arm toggle, turn budget
#   .claude/settings.local.json machine-local overrides
#   project-specific memories under memory/ (only universal feedback below syncs)
#   .codegraph/ node_modules/ dist/ .git/ src/ docs/ site/ __tests__/  (not workflow)
#
# FORCE-ONLY (leading '!' lines below): target runtime state — RIPER .riper-state,
#   branch memories, MEMORY.md. mixed/soft SKIP these (target's own state preserved);
#   only --force overwrites them (with backup) to hard-reset a target.
#
# GENERATED (not listed here): .mcp.json is written into each target root
#   (codegraph MCP), merge-preserving any sibling servers already there.

# --- Claude Code scaffolding ---
.claude/skills
.claude/commands
.claude/hooks
.claude/rules
.claude/agents
.claude/settings.json

# --- Other agents ---
.codex
.trae
.agents
.cursor

# --- Root instruction docs ---
CLAUDE.md
AGENTS.md
SKILL.md

# --- Universal feedback (user-global preference, applies to every project) ---
memory/feedback_korean_communication.md

# --- Force-only (project runtime state) — skipped in mixed/soft, overwritten only by --force ---
# Leading '!' = force-only. Protects each target's own RIPER state / memories from a routine
# sync; --force still overwrites (with backup). '.riper-state' lives under memory-bank/ → covered.
!.claude/memory-bank
!memory/MEMORY.md

# Session — Markdown Vector DB 통합 (mdAddOn) — Step 10 진행 중

- **Date**: 2026-06-06
- **Branch**: mdAddOn
- **RIPER**: MODE=EXECUTE
- **PLAN_FILE**: `.claude/memory-bank/mdAddOn/plans/mdAddOn-2026-06-02-markdown-vectordb-integration.md`
- **Steps 1~9 상세**: `.claude/memory-bank/mdAddOn/20260605-session.md` (이 파일은 Step 10 재개에 집중)

## 목표

마크다운을 sqlite-vec(vec0)+로컬 MiniLM(384d)로 인덱싱해 **codegraph.db 단일 파일**에 통합, MCP output 편입. 코드 그래프 무회귀.

## 진행 상황 (10 스텝 중 9 완료, Step 10 진행 중)

- ✅ **Step 1~7** (커밋됨): config/vec/parse/embed/indexer/scan-files + 스키마 마이그v5 + `codegraph_docs` MCP 툴. (커밋 `8169818`까지)
- ✅ **Step 8** (미커밋): `src/mcp/tools.ts` — context(시맨틱 searchDocs `## Related docs`)/node(governing cap2)/impact(governing cap3) 게이트 보강. 헬퍼 `buildRelatedDocsSection`(async)+`buildGoverningDocsSection`(동기). 부재 시 `''`→byte-identical.
- ✅ **Step 9** (미커밋): `server-instructions.ts`(`getServerInstructions(docsEnabled)`+`DOCS_INSTRUCTIONS`, 조건부), `proxy.ts`/`session.ts`(initialize 두 곳 `getServerInstructions(docsEnvOverride()===true)`), `CHANGELOG.md`([Unreleased] New Features 2불릿), `package-lock.json`(동기화 +954행, `npm ci --dry-run` exit 0). copy-assets는 schema.sql 이미 복사라 무변경.
- ⏳ **Step 10 진행 중**: `__tests__/docs-*.test.ts` 4종 작성 + `npm test` green.
  - ✅ `__tests__/docs-parse.test.ts` 작성 완료 (parseDoc 4종 + BLK_RE, 순수·항상 실행). **미커밋/untracked**.
  - ⏳ TODO: `docs-db.test.ts`, `docs-search.test.ts`, `docs-tool-gating.test.ts`.
  - ⏳ TODO: deps 설치(아래) → `npm run build` + `npm test`.

## 미커밋 변경 전체 (작업 트리)

```
 M .claude/memory-bank/mdAddOn/20260605-session.md
 M CHANGELOG.md
 M package-lock.json
 M src/mcp/proxy.ts
 M src/mcp/server-instructions.ts
 M src/mcp/session.ts
 M src/mcp/tools.ts            ← Step 8
?? __tests__/docs-parse.test.ts ← Step 10
```
(Step 8 + 9 + 10-부분이 모두 미커밋. 사용자가 단계별 리뷰 중, 커밋 미요청.)

## ⚠️ 환경 (CRITICAL — Step 10 실행 전 필독)

- **node_modules가 optional/native 없이 설치됨**: `npm install --ignore-scripts --omit=optional --no-audit --no-fund --no-package-lock` (61 pkg). 결과:
  - `@xenova/transformers` **없음** (optional)
  - **sqlite-vec native 없음** (`sqlite-vec-windows-x64`가 sqlite-vec의 optional dep → omit됨) → `isVecAvailable()` **false** 예상
  - **rollup native 없음 가능성** → **vitest가 안 돌 수 있음** (`@rollup/rollup-win32-x64-msvc` optional)
- **`npm test` 돌리려면**: 풀 설치 필요 → `npm install` (scripts+optional 포함). 이러면 rollup native(vitest) + sqlite-vec native 확보. @xenova 모델은 런타임 다운로드(테스트에선 embed 모킹으로 회피).
- **codegraph MCP 사용 불가**: 이 프로젝트 `.codegraph/` 미init. 탐색은 Read/Glob/Grep.
- tsconfig는 `__tests__/` 제외 → 테스트는 vitest/esbuild가 트랜스파일(타입체크 안 함). `tsc --noEmit`엔 영향 없음(현재 exit 0). 기존 테스트도 `res.content[0].text` 무가드 사용 = relaxed 스타일 OK.

## 나머지 3개 테스트 설계 (작성 그대로 구현)

### docs-db.test.ts
- import: `createDatabase` ('../src/db/sqlite-adapter'), `runMigrations`/`getCurrentVersion`/`CURRENT_SCHEMA_VERSION` ('../src/db/migrations'), `loadVecExtension`/`isVecAvailable`/`floatBlob` ('../src/docs/vec'), `ensureVectorTable` ('../src/docs/indexer'), fs/path.
- SCHEMA = `fs.readFileSync(path.join(process.cwd(),'src/db/schema.sql'),'utf-8')`.
- helper `tableExists(db,name)`: `SELECT name FROM sqlite_master WHERE type='table' AND name=?`.
- **마이그 v5 (항상)**:
  - fresh: `createDatabase(':memory:')`, `db.exec(SCHEMA)`, `runMigrations(db,getCurrentVersion(db))` → `getCurrentVersion(db)===CURRENT_SCHEMA_VERSION(5)`, `tableExists(mdast_metadata)`.
  - v4→v5: `:memory:`, 수동 `CREATE TABLE schema_versions(version INTEGER PRIMARY KEY, applied_at INTEGER NOT NULL, description TEXT)` + `INSERT ... VALUES(4,0,'sim')`; expect `!tableExists(mdast_metadata)`; `runMigrations(db,4)`; expect `tableExists` + version===5.
- **vec0 (`describe.skipIf(!isVecAvailable())`)**: `loadVecExtension(db)===true`, `ensureVectorTable(db)`, 합성벡터 2개 `new Float32Array(384)` (v1[0]=1, v2[1]=1) INSERT `mdast_vectors(rowid,embedding)` with **BigInt(rowid)** + `floatBlob`. KNN: `SELECT rowid,distance FROM mdast_vectors WHERE embedding MATCH ? AND k=2 ORDER BY distance` bind `floatBlob(q)` (q[0]=1) → `rows.length===2`, `Number(rows[0].rowid)===1`. floatBlob 바이트수 === 384*4.

### docs-search.test.ts
- **TOP (hoisted)**: `vi.mock('../src/docs/embed', () => ({ embed: async()=>{const v=new Float32Array(384);v[0]=1;return v;}, isEmbedAvailable: async()=>true, embedBatch: async()=>[], getEmbedder: async()=>null }))`.
- import: `searchDocs`/`findGoverningDocs` ('../src/docs/search'), `setDocsEnabled` ('../src/docs/config'), `loadVecExtension`/`isVecAvailable`/`floatBlob` ('../src/docs/vec'), `ensureVectorTable` ('../src/docs/indexer'), `DatabaseConnection` ('../src/db'), fs/path/os.
- **env 위생**: save/delete/restore `process.env.CODEGRAPH_DOCS` (beforeEach delete, afterEach restore). 모든 enable은 `setDocsEnabled(db,true)`(persisted)로 — env 미사용.
- temp db: `DatabaseConnection.initialize(path.join(dir,'t.db'))` → `.getDb()`; afterEach `conn.close()` + rmSync.
- **findGoverningDocs (deps 불필요)**: mdast_metadata INSERT (file_path 'docs/foo.md', code_refs `JSON.stringify(['src/foo.ts'])`), `setDocsEnabled(db,true)`; `findGoverningDocs(db,['src/foo.ts'])`→1건(file 'docs/foo.md'); `['src/other.ts']`→`[]`; `setDocsEnabled(db,false)`→`[]`.
- **searchDocs disabled (deps 불필요)**: 플래그 없음 → `searchDocs(db,'x')` → `{enabled:false, hits:[]}`.
- **searchDocs e2e (`describe.skipIf(!isVecAvailable())`, 모킹 embed)**: `loadVecExtension(db)`, `ensureVectorTable(db)`, mdast_metadata INSERT(code_refs ['src/foo.ts']) → id 조회 → `mdast_vectors(rowid,embedding)` INSERT BigInt(id)+floatBlob(v[0]=1), nodes INSERT(id 'n1',kind 'function',name 'fooFn',qualified_name 'fooFn',file_path 'src/foo.ts',language 'typescript',start_line 10,end_line 20,start_column 0,end_column 0,updated_at 0), `setDocsEnabled(db,true)`; `searchDocs(db,'foo')` → `enabled&&available`, `hits[0].file==='docs/foo.md'`, codeRefs⊇'src/foo.ts', symbols에 fooFn.

### docs-tool-gating.test.ts
- 패턴: `mcp-staleness-banner.test.ts` 참고. import: `CodeGraph` ('../src' named), `ToolHandler` ('../src/mcp/tools'), `setDocsEnabled` ('../src/docs/config'), fs/path/os.
- **env 위생** CODEGRAPH_DOCS.
- beforeEach: mkdtemp + `src/foo.ts` = `export function fooFn() { return 1; }`; `cg=CodeGraph.initSync(dir,{config:{include:['**/*.ts'],exclude:[]}})`; `await cg.indexAll()`; `handler=new ToolHandler(cg)`. afterEach: `cg.close()` + rmSync.
- helper: `txt = (await handler.execute(name,args)).content[0].text`.
- **test1 (docs OFF)**: node/impact/context(fooFn) → 텍스트에 `'Related docs'` **없음**. (OFF면 context도 searchDocs 조기반환 → embed 미호출, 안전.)
- **test2 (docs ON + governing)**: `setDocsEnabled(cg.getDb(),true)` + mdast_metadata INSERT governing 'src/foo.ts' (file 'docs/foo.md', code_refs ['src/foo.ts']); node → `'### Related docs'` + `'docs/foo.md'`; impact → `'### Related docs'`.
- **test3 (docs ON + governing 다른 파일)**: governing 'src/other.ts'; node(fooFn) → `'Related docs'` **없음** (byte-identical 게이트 증명).
- **⚠️ CRITICAL**: docs ON 상태에서 **handleContext 호출 금지** (searchDocs→embed→모델다운로드). ON일 땐 node/impact만. context는 OFF에서만 검증.

### (선택) instructions 유닛테스트
- `getServerInstructions(false)===SERVER_INSTRUCTIONS`; `getServerInstructions(true)` 는 `'codegraph_docs'`/`'Markdown docs'` 포함 + SERVER_INSTRUCTIONS로 시작. docs-tool-gating나 별도 파일에 추가 가능.

## Step 10 실행 순서 (재개)

1. 나머지 3개 테스트 파일 작성 (위 설계대로).
2. **풀 설치**: `npm install` (scripts+optional 포함; rollup native+sqlite-vec native 확보). 시간/네트워크 소요.
3. `npm run build` (tsc+copy-assets) green 확인.
4. `npx vitest run __tests__/docs-parse.test.ts __tests__/docs-db.test.ts __tests__/docs-search.test.ts __tests__/docs-tool-gating.test.ts` → green (vec0 deps 없으면 skipIf로 skip, 그래도 green).
5. (가능시) 전체 `npm test` 회귀 확인.
6. **수동검증** (플랜 성공기준): `CODEGRAPH_DOCS=1` 이 repo 인덱싱(`npm run cli -- index --with-docs` 등) → `codegraph_docs` 질의 + node/impact governing 게이트 확인. (단 @xenova 모델 최초 다운로드 필요.)

## 핵심 API/스키마 레퍼런스

- `createDatabase(p):{db,backend}` (src/db/sqlite-adapter); `DatabaseConnection.initialize(p)`/`.getDb()`/`.close()` (src/db); `runMigrations(db,from)`/`getCurrentVersion(db)`/`CURRENT_SCHEMA_VERSION=5` (src/db/migrations).
- `parseDoc`/`BLK_RE` (src/docs/parse); `loadVecExtension(db)`/`isVecAvailable()`/`floatBlob(vec)` (src/docs/vec); `embed`/`isEmbedAvailable` (src/docs/embed); `ensureVectorTable(db)`/`indexMarkdown` (src/docs/indexer).
- `searchDocs(db,q,opts)`/`findGoverningDocs(db,paths)`/`DocHit`/`GoverningDoc` (src/docs/search); `resolveDocsEnabled`/`setDocsEnabled`/`docsEnvOverride`/`EMBED_DIM=384`/`DOCS_ENV_VAR='CODEGRAPH_DOCS'`/`DOCS_META_KEY='docs_enabled'` (src/docs/config).
- `getServerInstructions(bool)`/`SERVER_INSTRUCTIONS` (src/mcp/server-instructions); `ToolHandler(cg|null).execute(name,args)→{content:[{text}],isError}` (src/mcp/tools); `CodeGraph.initSync(dir,{config})`/`.init(dir,{index})`/`.indexAll()`/`.getDb()`/`.close()` (src).
- **mdast_metadata 컬럼**: `id`(PK=vec0 rowid), `file_path`(UNIQUE), `blk_tags`, `code_refs`(JSON), `content_summary`, `content_hash`, `last_updated`. **mdast_vectors**(런타임 vec0): `rowid`(=id, BigInt 바인딩), `embedding` float[384].
- **nodes 컬럼**: `id,kind,name,qualified_name,file_path,language,start_line,end_line,start_column,end_column,docstring,signature,...,updated_at`. node:sqlite는 number를 REAL 바인딩 → vec0 rowid INSERT는 **BigInt 필수**(KNN `k`는 정수 리터럴 인라인).

## 게이트 (RULE-6, 중·대형)
- Step 10 완료 후 quality-sentinel + reporter 게이트 권장 (사용자 요청 시). 현재까지 인라인 검증(tsc, diff 리뷰)만 수행. 커밋은 사용자 미요청.

# Memory Bank: Obsidian-Graph-Vector-DB Session (2026-06-15)

## Context Information
- **Date**: 2026-06-15
- **Branch**: Obsidian-Graph-Vector-DB
- **Latest Commit**: 9134792 최신화
- **Working Directory Status**: clean (session deliverables committed)

## Session Summary
두 갈래 작업: (1) `/try` 타당성 분석 1건, (2) **codegraph MCP 도구가 세션에 안 뜨던 문제**의 근본 원인 추적 + 등록 수정 + 설명 문서화.

---

## 1. `/try` — init/index 제외 규칙을 별도 ignore 파일로 (분석만, 구현 X)
- 산출물: `docs/try/try_002.md` (6섹션 분석).
- 핵심 결론: 제외 로직 단일 chokepoint = `buildDefaultIgnore(rootDir)` (`src/extraction/index.ts:198-207`), 소비처 3곳(git 경로 `:296`, 워크 경로 `:529`, watcher `src/sync/watcher.ts:173`).
- **빼기(추가 제외)** 방향이면 `buildDefaultIgnore`에 `.codegraphignore` 로드 1줄 → 3경로 자동 반영(저위험). **gitignore 완전 대체/되살리기**면 `git ls-files` 빠른 경로 포기 필요(고위험, issue #407 git-first 전략 회귀).
- git 경로는 `.gitignore`를 직접 파싱하지 않고 `git ls-files`에 위임 → 별도 파일은 subtract만 가능, re-include 불가가 핵심 제약.

---

## 2. MCP 도구 미등록 문제 — 3계층 근본 원인

증상: `select:mcp__codegraph__codegraph_status,...` → "No matching deferred tools found". 사용자 가설: `@colbymchenry`→`@evespimrose` rename이 MCP 통신을 깸.

### 계층 1 — 등록 누락 (실제 블로커) · ✅ 수정 완료
- `~/.claude.json`에 codegraph MCP 서버가 **어디에도 등록 안 됨**: 프로젝트 키가 **path-format 중복**으로 둘 존재 (`"D:/Unity/codegraph"` mcpServers `{}`, `"D:\\Unity\\codegraph"` mcpServers 없음), 최상위 전역 `mcpServers`도 없음, `.mcp.json`도 없음.
- 빌드 clean, 글로벌 bin 정상(`@evespimrose/codegraph@0.9.8.1`, `which codegraph` OK, project version 일치) → 바이너리·코드 문제 아님. **에이전트 config 배선 문제**.
- 설치자 기준 정규 항목: `getMcpServerConfig()` = `{ type:'stdio', command:'codegraph', args:['serve','--mcp'] }` (`src/installer/targets/shared.ts:19-24`), 서버 키 하드코딩 `codegraph` (`claude.ts:85,235`).
- **적용한 수정 (사용자 선택: 옵션 2+3 동시):**
  - 생성: `D:\Unity\codegraph\.mcp.json` (커밋 대상, gitignore 안 됨).
  - 추가: `~/.claude.json` **최상위** `mcpServers.codegraph` (전역). Claude Code 라이브 재직렬화에도 **보존 확인**(키 순서만 재정렬).
  - 둘 다 `command: codegraph` → @evespimrose 글로벌 bin 사용 (rename 유지).
- 검증: `initialize`+`tools/list` 핸드셰이크 직접 수행 → 서버 정상, 도구 반환 확인.

### 계층 2 — tiny-repo 도구 게이트 (설계상, 버그 아님) · 미결정
- `ToolHandler.getTools()` (`src/mcp/tools.ts:818-854`): 인덱싱 파일 수 < `TINY_REPO_FILE_THRESHOLD`(**500**)이면 **핵심 5개 + docs**만 노출.
  - 노출: search, context, node, explore, trace (+docs if enabled)
  - 숨김: **callers, callees, impact, status, files, backlinks**
- **이 레포 = 217 files < 500 → 게이트 발동.** 핸드셰이크가 정확히 6개 반환(search/context/docs/node/explore/trace). `codegraph_status`(사용자가 물은 것)가 숨김 대상이라 등록 고쳐도 안 뜸.
- 근거(코드 주석 n=2 A/B): 3-tool은 cobra/ky/sinatra 회귀, 1-tool은 express -43%WIN→+107%LOSS, 5가 실측 하한. ITER4에서 임계값 150→500 상향(sinatra 159, slim ~200).
- 성질: "숨김=존재하지않음"(거부 아님), 런타임·동적(레포 크기 의존), **allowlist로 우회 불가**(게이트가 allowlist 뒤 무조건 적용), `getStaticTools()`엔 게이트 없음(오픈 후 `getTools()`에만).
- 글로벌·로컬 dist 모두 12개 **정의**는 함 — 정의 12, 노출 6.

### 계층 3 — rename은 직접 원인 아님
- 소스에 `colbymchenry` 깨진 참조 없음(스킬 docs/LICENSE/eval 스크립트 잔재뿐). 재설치 churn이 `mcpServers` 블록을 비운 것이 실제 메커니즘.

---

## 3. 산출 문서
- `docs/try/try_002.md` — ignore 파일 분리 타당성 분석.
- `docs/tiny-repo-tool-gating.md` — 게이트 정의/목적/동작/2갈래 선택지(A 재시작만 · B opt-in 오버라이드 · **절충안=B+레포로컬 env**)/영향 정리.

## 4. Next Steps (미결)
- **재시작 필수**: MCP config는 세션 시작 시 로드 → 현재 세션 미반영. Claude Code 재시작 + `.mcp.json` 신뢰 프롬프트 승인 → 핵심 6개 `mcp__codegraph__*` 노출.
- **계층 2 결정 대기**: status 등 6개도 노출할지.
  - 선택 시 B/절충안이면 `tools.ts:848`을 `if (!allToolsOverride() && stats.fileCount < THRESHOLD)`로 최소 변경 → `npm run build` → `sync-global-codegraph`(글로벌 bin EEXIST 충돌 주의) → config `env`에 `CODEGRAPH_ALL_TOOLS=1` 추가.
  - 권장: **절충안**(배포 기본 게이트 유지, 이 개발 레포 `.mcp.json`에만 env로 풀 표면).
- (참고) `.mcp.json`이 커밋되면 published 포크 clone 시 함께 배포됨 — 의도된 선택(옵션 2).
